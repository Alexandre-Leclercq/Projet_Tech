Environnement OK
Agent  	--> Passif --> Politique basique 
		   -->		aléatoire

	--> Actif --> Exploitation vs Exploration
		  --> Q learning


Fonction pour l'environnement :

- Step : donne l'action a et l'etat s réalise 
	 la transition vers s'
	 => Observation, Reward(s'), Donestage

- Reward R(s, a, s')
	 E(non final) = -1 	E(final)=1000 
	 différents que pour les état finaux

- Reset(seed) : gènére un nouvel environnement 
		à son état initiale et change la seed



Attention  : 
- ajout de la seed dans shuffle
- Quand on prends un mur bien mettre la reward



Agent 
-- classe agent 
	* historique d'exploration ? [[s,a,r],...]

-- choix de politique
-- code la politique basique 
(tout le temps en haut quand on peut plus droite)

--code politique aléatoire 
(avec un random)



Agent 				|Environnement
action				|	Step(s,a)
				|	=> observation() reward() done(boolean)
pi(s) = action conseillé	|
				|	Reset(seed)
utility				|	=> Observation(s0)
				|
				|		
Stock de l'exploration		|
[s',r,us]			|
				|
				|
				|
				|
				|
				|
				|
				|
				|


















